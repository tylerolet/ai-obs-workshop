# Mock safety model — returns "safe" for all requests.
# Lightweight stand-in for Llama-Guard (no GPU needed).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-safety
  namespace: daystrom-mini
  labels:
    app: mock-safety
    app.kubernetes.io/part-of: daystrom-mini
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mock-safety
  template:
    metadata:
      labels:
        app: mock-safety
    spec:
      containers:
        - name: mock-safety
          image: daystrom-mini/mock-server:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: chaos-config
          env:
            - name: PORT
              value: "8000"
            - name: MOCK_ROLE
              value: "safety-model"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mock-safety
  namespace: daystrom-mini
spec:
  selector:
    app: mock-safety
  ports:
    - port: 8000
      targetPort: 8000
